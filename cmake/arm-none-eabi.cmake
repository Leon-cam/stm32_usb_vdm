set(CMAKE_SYSTEM_NAME Generic)
set(CMAKE_SYSTEM_PROCESSOR ARM)

set(TOOLCHAIN_PREFIX arm-none-eabi-)
set(CMAKE_TRY_COMPILE_TARGET_TYPE STATIC_LIBRARY)

if(DEFINED TOOLCHAIN_ROOT)
    if(WIN32)
        set(_EXE ".exe")
    else()
        set(_EXE "")
    endif()
    set(TOOLCHAIN_BIN "${TOOLCHAIN_ROOT}/bin")
    set(CMAKE_C_COMPILER   "${TOOLCHAIN_BIN}/${TOOLCHAIN_PREFIX}gcc${_EXE}")
    set(CMAKE_CXX_COMPILER "${TOOLCHAIN_BIN}/${TOOLCHAIN_PREFIX}g++${_EXE}")
    set(CMAKE_ASM_COMPILER "${TOOLCHAIN_BIN}/${TOOLCHAIN_PREFIX}gcc${_EXE}")
    set(CMAKE_OBJCOPY      "${TOOLCHAIN_BIN}/${TOOLCHAIN_PREFIX}objcopy${_EXE}")
    set(CMAKE_SIZE         "${TOOLCHAIN_BIN}/${TOOLCHAIN_PREFIX}size${_EXE}")
else()
    if(NOT CMAKE_C_COMPILER)
        find_program(CMAKE_C_COMPILER   ${TOOLCHAIN_PREFIX}gcc)
        find_program(CMAKE_CXX_COMPILER ${TOOLCHAIN_PREFIX}g++)
        find_program(CMAKE_ASM_COMPILER ${TOOLCHAIN_PREFIX}gcc)
        find_program(CMAKE_OBJCOPY      ${TOOLCHAIN_PREFIX}objcopy)
        find_program(CMAKE_SIZE         ${TOOLCHAIN_PREFIX}size)
    endif()
endif()

set(CMAKE_FIND_ROOT_PATH_MODE_PROGRAM NEVER)
set(CMAKE_FIND_ROOT_PATH_MODE_LIBRARY ONLY)
set(CMAKE_FIND_ROOT_PATH_MODE_INCLUDE ONLY)
